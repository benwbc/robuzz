{
  "name": "blockfeed",
  "private": true,
  "version": "1.0.0",
  "description": "BlockFeed — an X/Instagram-style social platform for the Roblox community, with verified badges and a real moderation system.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "seed": "npm run seed -w server",
    "dev": "concurrently -k -n SERVER,CLIENT -c red,cyan \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
